---------------- ---------------- In-Built Functions ---------------- ---------------- 


---------------- Dot Notation ---------------- 
1) scala> :paste
    // Entering paste mode (ctrl-D to finish)

    var a="dear students"
    var b=a.indexOf('u')
    print(b)

    // Exiting paste mode, now interpreting.

    7a: String = dear students
    b: Int = 7

2) scala> print(a.toUpperCase)
    DEAR STUDENTS


---------------- Infix Notation ---------------- 
1) scala> :paste
    // Entering paste mode (ctrl-D to finish)

    var b=a indexOf 'u'
    print(b)

    // Exiting paste mode, now interpreting.

    7b: Int = 7

2) scala> print(a toLowerCase)
    warning: one feature warning; for details, enable `:setting -feature' or `:replay -feature'
    dear students

3) scala> print(a toUpperCase)
    warning: one feature warning; for details, enable `:setting -feature' or `:replay -feature'
    DEAR STUDENTS


---------------- ---------------- User-Defined Functions ---------------- ---------------- 


